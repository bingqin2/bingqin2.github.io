apiVersion: v1
kind: Service
metadata:
  name: bingqin-site
  labels:
    app.kubernetes.io/name: bingqin-site
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bingqin-site
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
